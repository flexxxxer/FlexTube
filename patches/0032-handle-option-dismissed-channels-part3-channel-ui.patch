---
 .../libretube/ui/fragments/ChannelFragment.kt | 29 +++++++++++------
 .../ui/sheets/ChannelOptionsBottomSheet.kt    | 32 +++++++++++++++++++
 2 files changed, 52 insertions(+), 9 deletions(-)

diff --git a/app/src/main/java/com/github/libretube/ui/fragments/ChannelFragment.kt b/app/src/main/java/com/github/libretube/ui/fragments/ChannelFragment.kt
index 3559a9001..d72c9eeb0 100644
--- a/app/src/main/java/com/github/libretube/ui/fragments/ChannelFragment.kt
+++ b/app/src/main/java/com/github/libretube/ui/fragments/ChannelFragment.kt
@@ -7,6 +7,7 @@ import androidx.core.os.bundleOf
 import androidx.core.view.isGone
 import androidx.core.view.isVisible
 import androidx.fragment.app.Fragment
+import androidx.fragment.app.activityViewModels
 import androidx.lifecycle.lifecycleScope
 import androidx.navigation.fragment.navArgs
 import androidx.recyclerview.widget.RecyclerView
@@ -29,11 +30,14 @@ import com.github.libretube.ui.adapters.VideosAdapter
 import com.github.libretube.ui.base.DynamicLayoutManagerFragment
 import com.github.libretube.ui.dialogs.ShareDialog
 import com.github.libretube.ui.extensions.setupSubscriptionButton
+import com.github.libretube.ui.models.DismissedChannelsModel
 import com.github.libretube.ui.sheets.ChannelOptionsBottomSheet
 import com.google.android.material.tabs.TabLayoutMediator
 import kotlinx.coroutines.Dispatchers
+import kotlinx.coroutines.flow.collectLatest
 import kotlinx.coroutines.launch
 import kotlinx.coroutines.withContext
+import kotlin.getValue
 
 class ChannelFragment : Fragment(R.layout.fragment_channel) {
     private var _binding: FragmentChannelBinding? = null
@@ -46,6 +50,7 @@ class ChannelFragment : Fragment(R.layout.fragment_channel) {
     private var isLoading = true
 
     private lateinit var channelContentAdapter: ChannelContentAdapter
+    private val dismissedChannelsModel: DismissedChannelsModel by activityViewModels()
 
     private var nextPages = Array<String?>(5) { null }
     private var isAppBarFullyExpanded: Boolean = true
@@ -145,16 +150,22 @@ class ChannelFragment : Fragment(R.layout.fragment_channel) {
             isSubscribed = it
         }
 
-        binding.showMore.setOnClickListener {
-            ChannelOptionsBottomSheet()
-                .apply {
-                    arguments = bundleOf(
-                        IntentData.channelId to channelId,
-                        IntentData.channelName to channelName,
-                        IntentData.isSubscribed to isSubscribed
-                    )
+        lifecycleScope.launch {
+            dismissedChannelsModel.dismissedChannels.collectLatest { list ->
+                val isDismissed = list.any { it.id == channelId }
+                binding.showMore.setOnClickListener {
+                    ChannelOptionsBottomSheet()
+                        .apply {
+                            arguments = bundleOf(
+                                IntentData.channelId to channelId,
+                                IntentData.channelName to channelName,
+                                IntentData.isSubscribed to isSubscribed,
+                                IntentData.dismissedChannel to isDismissed
+                            )
+                        }
+                        .show(childFragmentManager)
                 }
-                .show(childFragmentManager)
+            }
         }
 
         nextPages[0] = response.nextpage
diff --git a/app/src/main/java/com/github/libretube/ui/sheets/ChannelOptionsBottomSheet.kt b/app/src/main/java/com/github/libretube/ui/sheets/ChannelOptionsBottomSheet.kt
index 49840b72a..e019c9001 100644
--- a/app/src/main/java/com/github/libretube/ui/sheets/ChannelOptionsBottomSheet.kt
+++ b/app/src/main/java/com/github/libretube/ui/sheets/ChannelOptionsBottomSheet.kt
@@ -3,6 +3,7 @@ package com.github.libretube.ui.sheets
 import android.os.Bundle
 import android.util.Log
 import androidx.core.os.bundleOf
+import androidx.fragment.app.activityViewModels
 import com.github.libretube.R
 import com.github.libretube.api.MediaServiceRepository
 import com.github.libretube.constants.IntentData
@@ -13,8 +14,11 @@ import com.github.libretube.helpers.BackgroundHelper
 import com.github.libretube.helpers.NavigationHelper
 import com.github.libretube.obj.ShareData
 import com.github.libretube.ui.dialogs.ShareDialog
+import com.github.libretube.ui.models.DismissedChannelsModel
 import kotlinx.coroutines.Dispatchers
+import kotlinx.coroutines.flow.collectLatest
 import kotlinx.coroutines.withContext
+import kotlin.getValue
 
 /**
  * Dialog with different options for a selected video.
@@ -25,6 +29,8 @@ class ChannelOptionsBottomSheet : BaseBottomSheet() {
     private lateinit var channelId: String
     private var channelName: String? = null
     private var subscribed: Boolean = false
+    private var dismissed: Boolean = false
+    private val dismissedChannelsModel: DismissedChannelsModel by activityViewModels()
 
     override fun onCreate(savedInstanceState: Bundle?) {
         super.onCreate(savedInstanceState)
@@ -32,6 +38,7 @@ class ChannelOptionsBottomSheet : BaseBottomSheet() {
         channelId = arguments?.getString(IntentData.channelId)!!
         channelName = arguments?.getString(IntentData.channelName)
         subscribed = arguments?.getBoolean(IntentData.isSubscribed, false) ?: false
+        dismissed = arguments?.getBoolean(IntentData.dismissedChannel, false) ?: false
 
         setTitle(channelName)
 
@@ -42,6 +49,8 @@ class ChannelOptionsBottomSheet : BaseBottomSheet() {
             R.string.playOnBackground
         )
         if (subscribed) optionsList.add(R.string.add_to_group)
+        if (dismissed) optionsList.add(R.string.remove_dismissed_channel)
+        if (!dismissed) optionsList.add(R.string.add_dismissed_channel)
 
         setSimpleItems(optionsList.map { getString(it) }) { which ->
             when (optionsList[which]) {
@@ -96,6 +105,29 @@ class ChannelOptionsBottomSheet : BaseBottomSheet() {
                         Log.e(TAG(), e.toString())
                     }
                 }
+
+                R.string.add_dismissed_channel -> {
+                    try {
+                        withContext(Dispatchers.IO) {
+                            dismissedChannelsModel.add(channelId, channelName ?: "")
+                        }
+                    } catch (e: Exception) {
+                        Log.e(TAG(), e.toString())
+                    }
+                }
+
+                R.string.remove_dismissed_channel -> {
+                    try {
+                        dismissedChannelsModel.dismissedChannels.collectLatest { list ->
+                            val dismissedChannel = list.firstOrNull { it.id == channelId }
+                            if (dismissedChannel != null) withContext(Dispatchers.IO) {
+                                dismissedChannelsModel.delete(dismissedChannel)
+                            }
+                        }
+                    } catch (e: Exception) {
+                        Log.e(TAG(), e.toString())
+                    }
+                }
             }
         }
     }
-- 
2.53.0

